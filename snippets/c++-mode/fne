# -*- mode: snippet -*-
# name: find_end
# key: fne
# --
auto pos = std::find_end(std::begin(${1:haystack}), std::end($1), std::begin(${2:needle}), std::end($2));
if (pos != std::end($1)) {
    $3
}
$0
